<form method="post" action="{{ path('entry_details_delete', {'id': entry_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entry_detail.id) }}">
    <button class="btn">Delete</button>
</form>
