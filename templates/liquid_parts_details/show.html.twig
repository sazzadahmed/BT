{% extends 'base.html.twig' %}

{% block title %}LiquidPartsDetails{% endblock %}

{% block body %}
    <h1>LiquidPartsDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liquid_parts_detail.id }}</td>
            </tr>
            <tr>
                <th>WastageQuantity</th>
                <td>{{ liquid_parts_detail.wastageQuantity }}</td>
            </tr>
            <tr>
                <th>AvailableQuantity</th>
                <td>{{ liquid_parts_detail.availableQuantity }}</td>
            </tr>
            <tr>
                <th>SoldQuantity</th>
                <td>{{ liquid_parts_detail.soldQuantity }}</td>
            </tr>
            <tr>
                <th>SoldPrice</th>
                <td>{{ liquid_parts_detail.soldPrice }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ liquid_parts_detail.createAt ? liquid_parts_detail.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('liquid_parts_details_index') }}">back to list</a>

    <a href="{{ path('liquid_parts_details_edit', {'id': liquid_parts_detail.id}) }}">edit</a>

    {{ include('liquid_parts_details/_delete_form.html.twig') }}
{% endblock %}
